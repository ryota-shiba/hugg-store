# HUGG Website

HUGG brand website built with Next.js, Tailwind CSS, and static export.

## Development

```bash
npm install
npm run dev
```

Open `http://127.0.0.1:3000`.

## Build

```bash
npm run build
```

The project uses `output: 'export'` in `next.config.js`.
